# URL of the deployed FastAPI backend, e.g. https://your-backend.onrender.com
# Leave unset for local development — Vite's dev server proxies /api to
# http://localhost:8000 automatically (see vite.config.js).
VITE_API_URL=
